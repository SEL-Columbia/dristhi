Simple html javascript page that takes formhub's formJSON as input,
and converts it into a form definition JSON for use with drishti.

You can run 'python -m SimpleHTTPServer 8900' and access it via 
http://localhost:8900.
